export const metadata = {
  title: "Plan Measuring Tool",
  description: "Plan Measuring Tool frontend",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
